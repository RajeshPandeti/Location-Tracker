# Location-Tracker
Consent-based live location tracker using Python, HTML, CSS, and JavaScript. The dashboard generates a global invite link after deployment. A mobile user opens the link, allows GPS permission, and shares location. The dashboard tracks the latest location on a map using the phone number as an identifier.
